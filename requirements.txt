# Core
torch>=2.0
torchaudio

# Audio - LFM2 model
liquid-audio

# Audio I/O
sounddevice
numpy

# VAD - Silero VAD loaded via torch.hub (no pip package needed)

# Vision
ultralytics
opencv-python

# System monitoring
psutil

# Optional: Whisper for transcription (needed for --tools=keywords)
# pip install openai-whisper
# openai-whisper

# Optional: GPU acceleration (skip on Raspberry Pi)
# flash-attn
